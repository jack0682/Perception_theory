# Plan — 2026-04-01

---

## 현재 상태 요약

### 이미 완료된 수학 산출물
- `docs/03-31/proof/T-PERSIST-K-STRONG-MORSE-ATTEMPT.md`
  - strong regime를 단일 정리가 아니라 **theorem ladder**로 재구성
  - `T-Persist-K-Strong` 전체는 여전히 conjecture
  - compatible branch는 **conditional local persistence theorem**
  - negative overlap은 **local instability proposition**
  - merge branch는 **conditional proposition / conjecture**
- `docs/03-31/theory/THREE-REGIME-SYNTHESIS.md`
  - well-separated / weakly-interacting / strongly-interacting의 theorem-status map 고정
- `docs/03-31/theory/NEAR-BIFURCATION-LOCAL-THEORY.md`
  - near-bifurcation을 positive persistence theorem이 아니라
  - **restricted persistence / local non-persistence / deep-core remnant** 구조로 재정식화

### 문서/관리 반영 완료
- `docs/03-31/INDEX.md` 업데이트 완료
- `docs/00-overview.md` 헤더를 현재 상태(`I12`, `174 tests`)로 갱신 완료
- `CHANGELOG.md`에 2026-03-31 strong-regime / near-bifurcation 세션 반영 완료

### 아직 의도적으로 보류한 것
- `Canonical Spec v2.0.md`
  - theorem status를 실제로 승격/변경할 정당화가 생기기 전까지는 수정하지 않음
- 코드/실험 자동화
  - 현재 단계는 **코드보다 수학 문서 우선**

---

## 오늘 이어갈 핵심 수학 과제

### 1. Product-manifold basin/sublevel theorem on `Σ_M^K`
목표:
- single-formation의 basin/sublevel 논리를 `Σ_M^K`로 올린다.
- strong regime coexistence branch의 조건부 정리를 더 엄밀하게 받쳐줄 최소 정리 도출.

핵심 질문:
- joint local minimizer 주변의 sublevel connected component를 어떻게 정의할 것인가?
- lower-`K` competitor와의 barrier를 어떤 양으로 기술할 것인가?
- `μ_joint`, `μ_overlap`, `Δ_branch`의 관계를 어떤 형태로 정리할 것인가?

### 2. `(K-1)` merge competitor branch 구성
목표:
- strong-regime merge conjecture를 future theorem으로 올릴 수 있도록,
- 실제 **merge competitor branch**의 존재를 구성적으로 제시한다.

핵심 질문:
- overlap pair `(j,k)`를 하나의 merged field로 내리는 자연한 ansatz는 무엇인가?
- 기존 `K`-branch와 새 `(K-1)`-branch를 같은 sublevel geometry 안에서 비교할 수 있는가?
- competitor가 local minimizer인지, saddle 이후의 descending branch인지 어떻게 구분할 것인가?

### 3. Strong-regime selection theorem
목표:
- `μ_overlap ≤ 0`가 실제 merge branch 선택으로 이어지려면 필요한
- **selection / branch-choice** 조건을 분리해 명문화한다.

핵심 질문:
- gradient flow + reoptimization이 어느 branch를 선택하는가?
- strong-regime transport fixed-point 비유일성이 selection 문제에 어떻게 개입하는가?
- 어떤 최소 selection hypothesis를 두면 conditional merge proposition을 정식화할 수 있는가?

### 4. `exp24`와 near-bifurcation local theory 연결
목표:
- `exp24_basin_flow_test.py`를 마무리하고,
- near-bifurcation local theory의 핵심 명제와 직접 연결한다.

핵심 질문:
- 실제 gradient flow basin이 sublevel estimate보다 얼마나 큰가?
- near-bifurcation에서 isotropic radius가 무너질 때 directional survival이 남는가?
- deep-core remnant가 실험적으로 분리되는가?

---

## 우선순위
1. **`Σ_M^K` basin/sublevel theorem**
2. **merge competitor branch ansatz**
3. **selection theorem 가정 분리**
4. **`exp24` completion + local theory cross-check**
5. 그 다음에만 `Canonical Spec v2.0.md` 반영 검토

---

## 체크리스트

### 완료된 것
- [x] Strong regime 공존 branch를 theorem ladder 안의 조건부 정리로 재정식화
- [x] merge branch를 theorem이 아니라 conditional proposition / conjecture로 낮춤
- [x] near-bifurcation에서 실패하는 명제와 살아남는 명제를 분리
- [x] well-separated / weakly-interacting / strongly-interacting theorem-status map 고정

### 다음으로 닫아야 할 것
- [ ] `Σ_M^K` 위 basin / barrier / spectral gap 관계를 동일한 표기계로 통일
- [ ] strong-regime coexistence branch를 받치는 product-manifold basin/sublevel theorem 초안 작성
- [ ] `(K-1)` merge competitor branch의 구체적 구성 제시
- [ ] strong-regime selection hypothesis 최소 형태 도출
- [ ] `exp24` 결과를 near-bifurcation local theory에 연결

---

## 보류 / 주의
- strong regime merge는 아직 절대 `proved`로 쓰지 않음
- near-bifurcation에서는 positive persistence theorem으로 후퇴하지 않음
- `Canonical Spec v2.0.md`는 theorem status change가 실제로 정당화될 때만 수정
- 코드보다 수학 문서/정리 우선
