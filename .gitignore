# Python cache
__pycache__/
*.py[cod]
*.pyo

# Test cache
.pytest_cache/

# LaTeX auxiliary files
*.aux
*.bbl
*.blg
*.fls
*.log
*.out
*.toc
*.synctex.gz
texput.log
missfont.log

# macOS
.DS_Store

# CSV results (generated by experiments)
experiments/*.csv

# Local agent state (machine-local; never tracked)
.omc/
.omx/

# Per-developer agent guides (not part of repo authority)
AGENTS.md
# (CLAUDE.md is intentionally tracked — it is the project agent guide.)

# Frozen historical material (not edited)
_archive/

# Deleted root drafts (2026-05-04 audit cleanup; listed here so they cannot be re-introduced silently)
analyse_gemini.md
analyze_codex.md
deep-research-report.md
research_log.md
AUDIT_2026-04-18.md
